# easyrice

Generate terminal-style images from command output.

## Install

```bash
pip install easyrice-antojoha113
```

## CLI usage

```bash
easyrice --dest out.png --commands example_commands --config gnu_free_config --black
```

To render on top of an existing image, pass `--src` and omit `--black`.
